# QA 回归报告 — 会话 16

> **日期**：2026-05-23  
> **阶段**：M4 生产前最后一轮质量检查  
> **执行人**：会话 16  
> **清单依据**：`docs/qa-checklist.md`

---

## 汇总

| 类别 | 数量 |
|------|------|
| 通过 | 11 |
| 失败 | 0 |
| 阻塞（已解除） | 1 |

**【会话 16 完成】通过 11 项 / 失败 0 项 / 阻塞 1 项（已解除，非代码缺陷）**

---

## 1. 自动化执行记录

### 1.1 环境检查

| 项 | 结果 | 备注 |
|----|------|------|
| PostgreSQL 服务 | ✅ | e2e / pytest 均连库成功 |
| `.env` / DATABASE_URL | ✅ | 本地 `postgres@localhost:5432/capability_network` |
| Python 3.11 | ✅ | pytest 正常执行 |
| Node.js + npm | ✅ | `npm run build` 成功 |

### 1.2 数据库迁移

| 项 | 结果 | 备注 |
|----|------|------|
| `alembic upgrade head` | ⏭ 未单独重跑 | 历史迁移已应用；e2e 写入/读取正常 |

### 1.3 后端单测

```
cd backend && python -m pytest tests/ -q
...................                                                      [100%]
19 passed in 16.18s
```

**结果**：✅ **19/19 通过**

### 1.4 E2E Smoke（REST API）

**脚本变更**：`deploy/e2e-smoke.ps1` 第 6 步改为 `{ match_log_id }` 下单，并增加 `match_log_id` 空值断言。

**首次执行**：❌ 失败 — 匹配响应无 `match_log_id`（8000 端口为**旧进程**，未加载最新代码）。

**处理**：重启 `uvicorn app.main:app --host 127.0.0.1 --port 8000` 后重跑。

**第二次执行**：✅ **E2E PASSED**

```
5. Run matching
  match_log_id: 349ac38d-d848-4da9-8a7f-73a76668a4d5
6. Create deal + pay (via match_log_id)
  deal_id: d44c1a60-a061-455f-81ec-781c3c550b61 status: pending
  after pay: in_progress → delivered → completed
==> E2E PASSED
```

### 1.5 前端构建

```
cd frontend && npm run build
✓ 146 modules transformed.
✓ built in 1.54s
```

**结果**：✅ 通过（`vue-tsc -b && vite build` 无错误）

---

## 2. 浏览器手工用例（文字记录）

> 前端 dev server：`http://127.0.0.1:5173`（运行中）  
> 说明：SPA 路由守卫在客户端执行；以下结合 **路由代码审查** + **HTTP 探测** + **API E2E** 记录。

### 2.1 官网（未登录）

| # | 用例 | 结果 | 证据 |
|---|------|------|------|
| 1 | 未登录访问 `/` → 官网，不进登录页 | ✅ | `router/index.ts`：`/` 在 `MarketingLayout` 下，无 `requiresAuth`；`beforeEach` 仅对 `meta.requiresAuth` 重定向 |
| 2 | `/about` 可访问 | ✅ | HTTP 200；路由 `AboutView.vue` 已注册 |
| 3 | `/pricing` 可访问 | ✅ | HTTP 200；路由 `PricingView.vue` 已注册 |
| 4 | 未登录访问 `/app/offers` → 跳转登录 | ✅ | `beforeEach`：`requiresAuth && !token` → `{ name: 'login', query: { redirect } }` |

### 2.2 控制台全流程（登录后）

| # | 步骤 | 结果 | 备注 |
|---|------|------|------|
| 1 | 登录 → `/app` | ✅ | 默认 redirect `/app/offers`；guest 页有 token 时 redirect `/app` |
| 2 | 充值 | ✅ | e2e-smoke 充值 10000 分成功 |
| 3 | 发供给 → 发布 | ✅ | e2e-smoke seller offer publish |
| 4 | 发需求 | ✅ | e2e-smoke buyer intent create |
| 5 | 匹配 | ✅ | 返回 candidates + `match_log_id` + score_breakdown |
| 6 | 下单（match_log_id） | ✅ | deal 创建并关联 `match_log_id` |
| 7 | 支付 | ✅ | status → `in_progress` |
| 8 | 交付 | ✅ | status → `delivered` |
| 9 | 确认 | ✅ | status → `completed`；buyer frozen = 0 |

> **UI 层**：MatchingView 已优先 `{ match_log_id }` 创建成交（与 API 一致）。完整浏览器点击流建议在发版前再目视走一遍（双账号：买家 + 卖家）。

### 2.3 移动端 375px

| # | 用例 | 结果 | 证据 |
|---|------|------|------|
| 1 | Tab Bar 显示 | ✅ | `AppLayout.vue`：`@media (max-width: 768px)` 下 `.tab-bar { display: flex }`，sidebar 隐藏 |
| 2 | 四个 Tab 可切换 | ✅ | 供给 / 需求 / 订单 / 钱包 → `/app/offers|intents|deals|wallet` |
| 3 | 底部安全区 | ✅ | `padding-bottom: env(safe-area-inset-bottom)` |

---

## 3. 阻塞项

| ID | 描述 | 状态 | 处理 |
|----|------|------|------|
| B1 | 旧 uvicorn 进程响应缺少 `match_log_id`，e2e-smoke 下单失败 | **已解除** | 重启 backend；**非代码缺陷**。建议在 `deploy/local-windows.ps1 -Start` 或 CI 文档中强调部署后需重启/使用 `--reload` |

---

## 4. 已知非阻塞缺口

| 项 | 建议会话 |
|----|----------|
| `og-image.png` 缺失 | **12-收尾** |
| 宝塔 + HTTPS 生产部署 | **会话 9** |
| Git push + CI 首次绿灯 | **0-运维** |
| 场景包 scenes | **会话 10**（可选） |
| 深色模式 / 动效 polish | **会话 17**（可选） |

---

## 5. 本次交付物

| 文件 | 变更 |
|------|------|
| `docs/qa-checklist.md` | **新建** — 完整 QA 检查清单 |
| `docs/qa-report-20260523.md` | **新建** — 本报告 |
| `deploy/e2e-smoke.ps1` | **更新** — 使用 `match_log_id` 下单 + 空值校验 |

---

## 6. 建议下一步

1. **0-运维**：Git 建仓 push，触发 CI 验证 pytest + build 绿灯  
2. **12-收尾**：补 `og-image.png`  
3. **会话 9**：宝塔生产部署（有 Linux 服务器时）  
4. 发版前：浏览器双账号目视走一遍 UI 全流程（375px Tab Bar）
